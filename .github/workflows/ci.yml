name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  repository:
    name: Repository checks
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Check out latest rdrobust
        uses: actions/checkout@v4
        with:
          repository: rdpackages/rdrobust
          ref: main
          path: rdrobust-source

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Check repository layout
        shell: bash
        run: |
          test -f README.md
          test -f LICENSE.md
          test -f R/rdpower/DESCRIPTION
          test -f Python/rdpower/pyproject.toml
          test -f Python/rdpower/setup.cfg
          test -f stata/stata.toc
          test -f stata/rdpower.pkg

  python-build:
    name: Python build
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Check out latest rdrobust
        uses: actions/checkout@v4
        with:
          repository: rdpackages/rdrobust
          ref: main
          path: rdrobust-source

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build tools
        run: python -m pip install --upgrade pip build pytest

      - name: Install latest rdrobust from GitHub
        run: python -m pip install -e rdrobust-source/Python/rdrobust

      - name: Install Python package
        working-directory: Python/rdpower
        run: python -m pip install -e . --no-deps

      - name: Run Python tests
        working-directory: Python/rdpower
        run: python -m pytest

      - name: Build Python package
        working-directory: Python/rdpower
        run: python -m build

  r-check:
    name: R CMD check
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Check out latest rdrobust
        uses: actions/checkout@v4
        with:
          repository: rdpackages/rdrobust
          ref: main
          path: rdrobust-source

      - name: Set up R
        uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - name: Set up Pandoc
        uses: r-lib/actions/setup-pandoc@v2

      - name: Install R dependencies
        run: |
          repos <- c(RSPM = Sys.getenv("RSPM"), CRAN = "https://cloud.r-project.org")
          install.packages(c("ggplot2", "MASS", "broom", "gridExtra"), repos = repos)
        shell: Rscript {0}

      - name: Install latest rdrobust from GitHub
        run: R CMD INSTALL rdrobust-source/R/rdrobust

      - name: Build R source package
        run: R CMD build R/rdpower

      - name: Check R package
        run: R CMD check rdpower_*.tar.gz --no-manual
